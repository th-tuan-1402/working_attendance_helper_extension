import { PiniaPluginContext } from 'pinia'

export default function useLocalStorage({ store }: PiniaPluginContext) {
  // store.$subscribe((context) => {
  //   console.warn('mutation', context);
  // })

  // store.$onAction((context) => {
  //   console.warn('action', context);
  // })
}
